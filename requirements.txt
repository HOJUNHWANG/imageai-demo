gradio>=4.0.0

torch
torchvision

diffusers>=0.30.0
transformers
accelerate

timm
numpy
pillow
psutil
mediapipe

# Optional (recommended)
opencv-python

# NOTE: Segment Anything (SAM) is recommended to install via git:
#   pip install git+https://github.com/facebookresearch/segment-anything.git
